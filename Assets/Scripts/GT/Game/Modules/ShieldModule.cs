using GT.Game.Modules.Stats;

namespace GT.Game.Modules
{
    public class ShieldModule : BaseModule
    {
        public new void Initialize(ModuleStats stats)
        {
            base.Initialize(stats);
        }
    }
}